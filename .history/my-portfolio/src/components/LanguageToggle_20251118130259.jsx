import {useNavigate, useLocation } from 'react-router-dom';
import {cn} from '../lib/utils';

export const LanguageToggle = () => {
    // Bu component artık kullanılmıyor, navbar içine entegre edildi
    return null;
};
